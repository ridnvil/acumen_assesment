info:
  name: List Customer
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrlMock}}/customers?page=2&limit=10"
  params:
    - name: page
      value: "2"
      type: query
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
