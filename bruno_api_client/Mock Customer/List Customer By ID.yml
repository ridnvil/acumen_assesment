info:
  name: List Customer By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrlMock}}/customers/CUST-002"
  params:
    - name: pahe
      value: "1"
      type: query
      disabled: true
    - name: limit
      value: "10"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
