info:
  name: Ingest Customer
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/ingest?page=1&limit=25"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "25"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
