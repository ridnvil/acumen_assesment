info:
  name: List Customers By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/customers/CUST-001x"
  params:
    - name: page
      value: "1"
      type: query
      disabled: true
    - name: limit
      value: "10"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
