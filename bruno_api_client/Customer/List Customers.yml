info:
  name: List Customers
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/customers?page=1&limit=1"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
